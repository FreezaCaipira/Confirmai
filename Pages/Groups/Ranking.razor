@page "/grupo/{Id:int}/ranking"

@attribute [Authorize]

@using System.Security.Claims
@using Confirmai.Data
@using Confirmai.Enums
@using Confirmai.Models
@using Confirmai.Shared.Components.Groups
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.EntityFrameworkCore
@using Confirmai.Services.Core

@inject Confirmai.Services.Core.UiTextService Ui


<PageTitle>@Ui.Get("Group.RankingPageTitle", group?.Name ?? @Ui["Group.PageTitle"]) · Confirmai</PageTitle>

@if (isLoading)
{
    <div class="detail-loading">
        <div class="detail-skeleton">
            <div class="skeleton-header">
                <div class="skeleton-line skeleton-line--sm"></div>
                <div class="skeleton-line skeleton-line--lg"></div>
                <div class="skeleton-line skeleton-line--md"></div>
            </div>
            <div class="skeleton-section">
                <div class="skeleton-section-title"></div>
                <div class="skeleton-list">
                    <div class="skeleton-list-item">
                        <div class="skeleton-list-item-avatar"></div>
                        <div class="skeleton-list-item-content">
                            <div class="skeleton-line skeleton-line--md"></div>
                            <div class="skeleton-line skeleton-line--sm"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
else if (group is null || !isMember)
{
    <div class="detail-not-found">
        <i class="fas fa-lock detail-not-found-icon"></i>
        <h2>@Ui["Group.NotAuthorized"]</h2>
        <a href="/">← @Ui["Common.Back"]</a>
    </div>
}
else if (!group.EnablePostMatchRanking)
{
    <div class="detail-not-found">
        <i class="fas fa-toggle-off detail-not-found-icon"></i>
        <h2>@Ui["Group.FeatureNotEnabled"]</h2>
        <a href="/grupo/@group.Id">@Ui["Group.BackToGroup"]</a>
    </div>
}
else
{
    <div class="detail-page">
        <div class="detail-card">

            <header class="detail-header">
                <div class="detail-header-top">
                    <a href="/grupo/@group.Id" class="detail-back-link">
                        <i class="fas fa-arrow-left"></i> @Ui["Common.Back"]
                    </a>
                    <a href="/grupos" class="btn-view-groups">
                        <i class="fas fa-layer-group"></i> @Ui["Groups.Title"]
                    </a>
                </div>
                <div class="detail-header-badge">
                    <span class="detail-sport-badge">
                        @(group.Sport == Sport.Futsal ? @Ui["Group.SportFutsal"] : @Ui["Group.SportPoker"])
                    </span>
                </div>
                <h1 class="detail-title ranking-title"><i class="fas fa-trophy"></i> @Ui["Group.RankingTitle"]</h1>
            </header>

            @* ── Filtro ────────────────────────────────────────────────── *@
            <RankingViewSelector
                ViewMode="@viewMode.ToString()"
                CurrentYear="@currentYear"
                OnViewModeChanged="@HandleViewModeChanged" />

            @* ── Tabela ────────────────────────────────────────────────── *@
            <RankingTable
                RankingRows="@rankingRowsTable"
                CurrentUserId="@currentUserId" />

            <p class="ranking-footnote">
                <i class="fas fa-info-circle"></i>
                @Ui["Group.RankingFootnote"]
            </p>

        </div>
    </div>
}
