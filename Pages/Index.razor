@*
    Root URL ("/") is redirected to "/servers" via a server-side endpoint
    registered in Program.cs (app.MapGet("/", ...)). This component is no
    longer routed and is kept only to avoid breaking imports/tests that may
    still reference the file.
*@

@code {
}